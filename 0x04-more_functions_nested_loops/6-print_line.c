#include "main.h"

/**
 *  print_line - printing a line
 *
 * Return: void
 */

void print_line(int n)
{
        char a


        for (i = 1; i <= 10; i++)
        {
                for (j = 0; j <= 14; j++)
                {
                        if (j >= 14)
                                _putchar('1');
                        _putchar(j % 10 + '0');
                }
                _putchar('\n');
        }
}
