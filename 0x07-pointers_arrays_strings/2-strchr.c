#include "main.h"
/**
 * _strchr - Entry point
 * @s: input
 * @c: input
 *
 * Return: change array with new value for n bytes
 */

char *_strchr(char *s, char c)
{
int i = 0;

for (; s[i] >= '\0'; i++)
{
if (s[i] == c)
}
return (0);

}
