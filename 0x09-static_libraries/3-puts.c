#include "main.h"

/**
 *    _put - function that print a string
 *    @str: pointer to the string
 *    Return: string and new line
 */

void _puts(char *str)
{
        int i = 0;

        while (str[i])
        {
                (str[i];)
                ++ ('\n');
        }

}
