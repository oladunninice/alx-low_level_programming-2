#include "main.h"
#include <stdio.h>

/**
 * main - print its name
 * @argc: count
 * @argv: string of array
 * Return: always 0
 */

int main(int argc__attribute__((unused)), char *argv[])
{
printf("%d\n", argv);
return (0);
}
